[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @paperclipai/ui build"
  publish = "ui/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
